export class StudentGrade {
    constructor(id, indexNumber, grade, isValid) {
        this.id = id;
        this.indexNumber = indexNumber;
        this.grade = grade;
        this.isValid = isValid;
    }
}